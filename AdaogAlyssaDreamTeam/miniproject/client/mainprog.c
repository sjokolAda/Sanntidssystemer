#include <arpa/inet.h>
#include <pthread.h>
#include "miniproject.h"
#include <stdio.h>
#include <stdlib.h>

#define UDP_PORT = 9999;
#define SERVER_IP = 129.241.187.146;

struct udp_conn;
    udp_conn.sock = 
	sock
	struct sockaddr_in server;
	struct sockaddr_in client;
	socklen_t client_len;
};

int main()
{
    udp_init_client(struct udp_conn *udp, int port, char *ip);
    //start threads
    // connect to server
       
}




 int UDP_listener()
